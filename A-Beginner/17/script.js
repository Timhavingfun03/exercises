// Skriv en funktion `isTanja` som returnerar `true` ifall argumentet är strängen "Tanja"  och annars `false`

function isTanja(x){
    if(x === 'Tanja'){
        return true
    } else(x !== 'Tanja')
        return false
    }


console.log(isTanja('Tim'))

console.log(isTanja('Tanja'))